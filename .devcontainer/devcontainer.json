{
	"name": "resume",
	"build": {
		"dockerfile": "../Dockerfile"
	},
	"workspaceMount": "source=${localWorkspaceFolder},target=/resume,type=bind,consistency=delegated",
	"workspaceFolder": "/resume"
}